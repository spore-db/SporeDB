apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sporedb.fullname" . }}
  labels:
    {{- include "sporedb.labels" . | nindent 4 }}
data:
  sporedb.yml: |
    {{- .Values.sporedb.config | nindent 4 }}
